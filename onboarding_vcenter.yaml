- name: Create Folder for Onboarding User
  hosts: localhost
  become: false
  gather_facts: false
  
  tasks:
    - name: Create folder
      community.vmware.vcenter_folder:
        hostname: "{{ vcenter_hostname }}"
        username: "{{ vcenter_username }}"
        password: "{{ vcenter_password }}"
        validate_certs: "{{ vcenter_validate_certs }}"
        datacenter: "{{ vcenter_datacenter }}"
        folder_name: "{{ vcenter_destination_folder }}"
        folder_type: vm
        state: present
        parent_folder: "User"

    - name: Debug folder creation message
      debug:
        msg: "Folder '{{ vcenter_destination_folder }}' created successfully in datacenter '{{ vcenter_datacenter }}'" tasks:
    - name: Create Resource Pool
      community.vmware.vmware_resource_pool:
        hostname: "{{ vcenter_hostname }}"
        username: "{{ vcenter_username }}"
        password: "{{ vcenter_password }}"
        validate_certs: "{{ vcenter_validate_certs }}"
        datacenter: "{{ vcenter_datacenter }}"
        resource_pool: '{{ resource_pool_name }}'
        cluster: '{{ cluster_name }}'
        state: present
      register: resource_pool_creation_result

    - name: Display confirmation message
      debug:
        msg: "Resource Pool '{{ resource_pool_name }}' created successfully in datacenter '{{ vcenter_datacenter }}'."
      when: resource_pool_creation_result is changed  # Only show if the resource pool was created
