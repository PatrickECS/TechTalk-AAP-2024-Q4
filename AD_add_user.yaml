---
- name: Add user to Microsoft Active Directory and assign to group
  hosts: localhost
  gather_facts: yes

  tasks:
   - name: Ensure user Bob is present with address information
     microsoft.ad.user:
       identity: bob
       firstname: Bob
       surname: Smith
       company: BobCo
       password: B0bP4ssw0rd
       state: present
       groups:
         set:
           - ArrowLab-Users
